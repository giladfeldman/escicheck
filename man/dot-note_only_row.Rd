% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{.note_only_row}
\alias{.note_only_row}
\title{Build an extraction-only NOTE output row (no recomputation)}
\usage{
.note_only_row(row, message)
}
\arguments{
\item{row}{A single parsed row (from \code{flattened_rows_to_parsed()}).}

\item{message}{Uncertainty message explaining why the row is not verified.}
}
\value{
A one-row tibble with status "NOTE", check_scope "extraction_only".
}
\description{
v0.6.4: used for docpluck table rows that carry only a point estimate plus a
CI (and maybe p) with no test statistic (\code{test_type = "table_estimate"}).
Such a row cannot be independently recomputed, so it is surfaced as an honest
NOTE that reports the estimate / CI / p exactly as extracted. Column set
mirrors the \code{df_arity_mismatch} short-circuit row so it binds uniformly with
the main \code{compute_and_compare_one()} output.
}
\keyword{internal}
