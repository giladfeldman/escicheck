% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute.R
\name{ci_R2_all}
\alias{ci_R2_all}
\title{Compute CIs for R-squared via partial-eta-squared NCP F-inversion (v0.3.5)}
\usage{
ci_R2_all(R2, df1, df2, F_val = NA_real_, level = 0.95)
}
\arguments{
\item{R2}{R-squared (point estimate, in [0, 1))}

\item{df1}{Numerator df (number of predictors)}

\item{df2}{Denominator df (residual df, N - df1 - 1)}

\item{F_val}{Optional F statistic; computed from R2/df1/df2 if NA}

\item{level}{Confidence level}
}
\value{
Named list of ci_result objects with R2-tagged methods
}
\description{
R^2 in a single-omnibus regression (or a one-predictor model) is
mathematically identical to partial eta-squared. We route through
ci_etap2_all() and tag the method names so the matcher distinguishes
R^2-routed CIs from native eta_p^2 CIs. Caller supplies the F statistic
(when available) or back-computes from R^2 + df1 + df2.
}
\keyword{internal}
