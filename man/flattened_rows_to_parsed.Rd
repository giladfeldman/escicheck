% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse.R
\name{flattened_rows_to_parsed}
\alias{flattened_rows_to_parsed}
\title{Map docpluck structured table rows to parsed-statistic rows}
\usage{
flattened_rows_to_parsed(table_rows)
}
\arguments{
\item{table_rows}{A list of docpluck flattened-row records, each a list with
\code{label}, \code{row_label}, \code{row_idx}, and a \code{fields} list. NULL or empty returns
NULL.}
}
\value{
A tibble of parsed-statistic rows (or NULL), bindable to the
\code{parse_text()} output via \code{dplyr::bind_rows()}.
}
\description{
v0.6.4: consumes docpluck's \code{?structured=true} \code{flattened_rows[]} (typed
\code{fields}, REQUEST_11 / docpluck v2.4.95) and emits rows in the same shape
\code{parse_text()} returns, so the existing \code{compute_and_compare_one()} pipeline
verifies / routes them with no sentence re-parsing. Only rows whose \code{fields}
carry a recognised statistic are mapped; everything else is skipped (the same
safe no-op as an empty \code{fields}).
}
\details{
Mapping (typed keys only -- an effect family is never inferred from an
untyped \code{est}):
\itemize{
\item \code{t}   -> test_type "t" (df from \code{df}; Cohen's \code{d} bound when present)
\item \code{F}   -> test_type "F" (df1/df2 when present; the partial-eta^2 \code{est} is
left unbound -- docpluck emits it untyped, so p is verified but
the effect is not, pending docpluck's deferred \code{effect_type})
\item \code{r}   -> test_type "r" (N from \code{n})
\item \code{est} (no test statistic) -> test_type "table_estimate", an
extraction-only NOTE that surfaces est + CI + p (cannot be
independently recomputed from an estimate alone)
}

Reported CI bounds map to \code{ciL_reported} / \code{ciU_reported}; \code{p_op} to
\code{p_symbol}. Each row is tagged \code{from_table = TRUE} (so check.R sets
\code{result_context = "table"}) and carries \code{source_table} / \code{table_group}
(the docpluck arm tag: ITT/PP, Separate/Joint, Target article/Replication).
}
\keyword{internal}
