% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{.dedup_table_vs_prose}
\alias{.dedup_table_vs_prose}
\title{Drop docpluck table rows that duplicate a text-parsed (prose) row}
\usage{
.dedup_table_vs_prose(parsed)
}
\arguments{
\item{parsed}{A parsed-row tibble after table rows have been bound in.}
}
\value{
\code{parsed} with duplicate table rows removed (prose row kept).
}
\description{
v0.6.4: a table cell often restates a result already reported inline in the
body (e.g. a 90203 Table 9 F that also appears in the H5 paragraph, or a
PROSECCO Table 2 risk difference also stated in the abstract). Keeping both
double-counts and lets the table-derived NOTE drag the summary. This collapses
a \code{from_table} row when a prose row shares the same reported numeric signature.
}
\details{
The signature is the sorted, rounded set of the row's reported numbers
(\code{stat_value}, \code{effect_reported}, \code{ciL_reported}, \code{ciU_reported}) -- robust to
the same value landing in different columns across representations (a prose
\code{rdpct} puts the estimate in \code{stat_value}; a table estimate puts it in
\code{effect_reported}). A CI bound must be present for a row to participate, so a
bare statistic never collapses on a coincidental value match.
}
\keyword{internal}
